version: "3.7"
services:
  server:
    build: employment_task/
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - 8000:8000
    volumes:
      - ./:/app
  notify:
    build: 
      context: ./employment_task/
    command: python manage.py process_tasks
